# Minimal production requirements for backend (CPU-only, Render-friendly)
# Pinned for compatibility with Python 3.11
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.10.6
pydantic-settings==2.1.0
bcrypt==5.0.0
PyJWT==2.12.1
python-jose==3.3.0
python-dotenv==1.0.0
supabase==2.16.0
httpx==0.27.2
pydub==0.25.1
python-multipart==0.0.6
python-json-logger==4.0.0
requests==2.32.5
passlib==1.7.4

# Note: GPU/ML packages (torch, transformers, triton, xgboost, safetensors, etc.)
# and CUDA/NVIDIA packages intentionally removed for CPU-only deployment.
